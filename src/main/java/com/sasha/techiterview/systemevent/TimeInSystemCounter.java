package com.sasha.techiterview.systemevent;

import java.util.List;

/**
 * Implement the method {@link #calculateTimeInSystem(List)}.
 *
 * <p>
 * Requirements:
 *  1) For each user, find the durations of all sessions from all devices
 *  2) Return a List<User> sorted by total time in system, from longest to shortest
 *  3) `timeLogIn` and `timeLogOut` are strings in "HH:mm" format
 *  4) `timeLogOut` is always after `timeLogIn`
 */


public class TimeInSystemCounter {

    public List<User> calculateTimeInSystem(List<SystemEvent> events) {
        // implement solution here
        return null;
    }
}
